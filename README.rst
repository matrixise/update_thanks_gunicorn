update_thanks.py is for the gunicorn project.


.. sourcecode:: bash

    git log --format="%an <%ae>" | python update_thanks.py

You will get a `result.txt` file, after that, you can work with the file (update,
remove, ...)

